<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fmsirvent.ParallaxEverywhereSample" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.USE_CREDENTIALS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".App">

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activity.ParallaxOriginalActivity"
            android:label="@string/origin_parallax_actitivy_name" />

        <activity
            android:name=".activity.FeedImagesGridActivity"
            android:label="@string/staggeredgrid_parallax_activity_name" />

        <activity
            android:name=".activity.FeedImagesActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".activity.AboutActivity"
            android:label="@string/about_activity_name"
            android:theme="@style/AppTheme.Transparent"/>
    </application>

</manifest>
